# Expected outputs

This directory contains only redacted, deterministic examples used by release review.
It never contains a real case number, user ID, document token, URL, attachment text, or credential.

- `template-validation-example.json`: shape returned by the local template validator.
- `sample-case-manifest.json`: non-authoritative input shape for boundary review.
