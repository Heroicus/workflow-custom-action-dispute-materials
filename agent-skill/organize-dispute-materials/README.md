# organize-dispute-materials 6.9.0

一次读取并封印目标 Base 记录的全部附件。原生文本直接进入逐字语料。视觉任务只覆盖独立图片、无足够文字层的扫描页和有效嵌入图片，并通过真实附件会话交给纠纷材料视觉核验员。音频通过飞书妙记生成并远端读回逐字稿。纠纷材料整理专员唯一合并事实、生成固定 XML 报告并写回同一记录。

完成条件由代码强制执行：全部材料和证据包完整、候选报告本地和远端一致、上传人权限读回为 `full_access`、Base 阶段写回和最终写回一致、最终状态写入后再次读回报告与权限仍然一致。
