# 组件侧机械校验清单（3.3.3 记录 ID 唯一定位契约）

> 本清单取代旧的“只传案件编号”校验。原则：能用脚本校验的不靠 prompt；`record_id` 是唯一定位键，案件编号只做一致性核验。

## A. 提交组件（`src/index.ts` / `src/analysis.ts`）

| # | 校验 | 判据 |
|---|---|---|
| A1 | 输入身份完整 | 表单只保留必填的 `recordId`、`targetTableId`；不再要求重复映射案件编号。 |
| A2 | 目标记录预检 | 使用当前 Base + `table_id + record_id` GET 同一条记录，再从该记录读取案件编号、`上传材料`、`上传人` 和状态。 |
| A3 | 同记录状态门禁 | 写入 `状态=分析中` 并 GET 读回；未读回成功不创建 Aily chat。 |
| A4 | 单次任务投递 | 消息只包含 `RUNTIME_INPUT_JSON` 和短规则，不传附件二进制；同一在途记录不得创建第二个 chat。 |
| A5 | 错误可诊断 | 失败日志包含 build、stage、record 和安全错误摘要，并绑定同一 `record_id`；不得更新其他记录。 |
| A6 | Aily 连接器快速失败 | Skill 必须在第一步使用真实 Base 精确读取工具；工具缺失时返回 `BASE_CONNECTOR_UNAVAILABLE`，禁止 bash 搜索和长时间循环。 |

## B. Skill 侧必须做

| # | 校验 | 判据 |
|---|---|---|
| B1 | 精确读取 | 只能用生产 Base 配置 + `table_id + record_id` 读取；禁止案件编号搜索、模糊命中和历史记录。 |
| B2 | 当前附件边界 | 只枚举该记录当前 `上传材料`，逐个读取正文；文件名、大小或 token 不算正文。 |
| B3 | 原生文档 | 创建原生飞书云文档并读回结构；模板 DOCX 只能作版式参考，不能作为最终报告。 |
| B4 | 权限交付 | 读取该记录 `上传人.open_id`，以 `member_type=openid`、`perm=full_access` 授权并读回。 |
| B5 | 终态回写 | 同一记录写裸 `/docx/` URL、状态、完成时间、报告版本和日志，并最终读回；成功时失败字段为空。 |
| B6 | 技术失败 | 字段、附件、文档、权限或回读失败时清空链接，写 `状态=分析失败` 和短错误原因，不返回成功链接。 |

## C. 本地回归

| # | 测试 | 判据 |
|---|---|---|
| C1 | 组件全量测试 | `npm test` 全部通过，包含逻辑、action、Skill 契约和 ZIP 边界测试。 |
| C2 | Skill 全量测试 | `unittest 10/10`、模板篡改失败、符号链接拒绝、ZIP 根目录干净。 |
| C3 | 发布身份 | 组件日志出现 `build=3.3.3-record-id-only-dispatch`；Skill 包的 `SKILL.md` 与 `agents/openai.yaml` 同包同版。 |
| C4 | 线上门禁 | 未看到同一 `table_id + record_id` 的“精确读取 → 枚举附件 → 读取正文”调用前，不创建云文档、不创建新验收记录。 |

## D. 线上验收

只有以下条件全部满足才算闭环：

```text
组件同记录状态=分析中
→ 只创建一个 Aily chat
→ Agent 精确读取同一 record_id
→ 当前上传材料正文真实读取
→ 原生云文档创建并结构读回
→ 上传人 open_id/full_access 授权并读回
→ 同一记录裸 URL 回写并读回
→ 状态、时间、版本正确，失败字段为空
→ 上传人真实打开、编辑并管理协作者
```

组件上传回执、Skill 导入成功、Aily 返回自然语言、Base HTTP 200 和本地测试都不能单独替代上述证据。
